import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Customers",
};

export default function Page() {
  return <p>Customers Page</p>;
}
// test for deployment error
// remove in the last commit
